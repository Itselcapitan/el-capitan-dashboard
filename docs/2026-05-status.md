# Dashboard Status — May 5, 2026

## Updates shipped (yesterday + today)

### Yesterday — bug fixes that mattered
- **Fixed apples-to-oranges IG vs TikTok comparison.** The dashboard
  was comparing IG `avgLikes` to TT `avgPlays` (likes vs views),
  making TikTok always look 50–100x stronger. Reality: IG is
  outperforming TT 5x on actual reach (2,990 IG vs 654 TT avg
  plays). All "TikTok is your growth opportunity" recommendations
  before this date were built on the broken comparison.
- Added symmetric metrics: `avgReelViews` + `reelsScraped` on IG,
  `avgComments` + `avgShares` on TT, so future comparisons are
  apples-to-apples.
- Strategy prompt to Gemini now has an explicit cross-platform
  comparison rule preventing the same mistake.
- SoundCloud release dates surfaced in scrape logs (data was
  already in Firebase, just unprinted).

### Today — Strategy folder + Graph API integration

**Strategy folder created** at `strategy/playbooks/` with all
research outputs:
- `2026-q2-deep-research.md` — original 40-page playbook
- `2026-q2-verification-sweep.md` — fact-check of 10 load-bearing
  claims (RealityForge refuted, skip-rate verified, etc.)
- `2026-q2-john-summit-deep-dive.md` — full CTRL ESCAPE post-mortem
- `2026-q2-nyc-operational-map.md` — venues, bookers, outreach
  scripts, ticket-sales strategy for July 17 + August 1 gigs
- `2026-q2-trial-reels-operations-manual.md` — 30-day variant
  cycling test design

**Meta Graph API integration**:
- Created `el-capitan-dashboard-IG` Meta Developer app
- Linked `@itselcapitan_` IG Business Account to the El Capitán
  Facebook Page via Business Portfolio
- Generated non-expiring Page access token
- Stored as GitHub secrets: `FB_PAGE_ACCESS_TOKEN`,
  `IG_BUSINESS_ACCOUNT_ID`, `FB_PAGE_ID`

**New scraper**: `scripts/scrape-ig-insights.mjs`
- Pulls per-post insights for ~25 recent posts via Graph API
- Captures: reach, skip rate, saves, shares, follows-from-post,
  profile visits, avg watch time, plays, total interactions
- Branches metric set by media type (reels vs static)
- Writes to `analytics/latest/igInsights/{mediaId}` (PATCH so it
  merges, doesn't overwrite)
- Snapshots to `analytics/history/{date}/igInsights` for trends
- Aggregate summary at `analytics/latest/igInsightsSummary`

**New workflow**: `.github/workflows/scrape-ig-insights.yml`
- Standalone insights pull, runs 6:15am ET + 6pm ET daily
- Independent of Apify — won't fail if Apify hits a rate limit

**Existing `scrape.yml` updated** with `if: always()` on the
insights step so a failed Apify pull can't block Graph API.

### First findings from real Graph API data (today's run)
- Average reel skip rate: **64.8%** (Trial Reels target is <40%)
- Best hook: ChatGPT Punta Cana reel (54% skip)
- Worst hook: DANCE. release reel (76% skip)
- **One static post (4,616 reach) drove 12 of 18 recent
  follows-from-post** — biggest single growth lever in the catalog,
  needs investigation
- Total shares: 768 across 25 posts (strong viral signal)
- Total saves: 17 (genuinely low; replay-value gap)

---

## What you now have access to in Graph API

### Per-post insights (LIVE — already pulling)
| Metric | What it tells you |
|---|---|
| `reach` | Unique accounts reached |
| `views` / `plays` / `video_views` | View counts |
| `saved` (saves) | Replay-value signal |
| `shares` | Viral signal |
| `reels_skip_rate` | % skipped in first 3 sec — REELS only — diagnostic for hook strength |
| `ig_reels_avg_watch_time` | Avg watch duration ms — REELS only |
| `ig_reels_video_view_total_time` | Total view time across all viewers |
| `clips_replays_count` | How many times the reel looped |
| `follows` | Follows-from-post (true conversion metric) |
| `profile_visits` | Profile visits from this specific post |
| `profile_activity` | Bio link clicks + tags + email clicks |
| `total_interactions` | Sum of all engagement |
| `reposts` | Reposts to other public profiles (excludes DMs) |
| `comments` / `replies` | Comment counts |
| `likes` / `total_likes` | Likes |
| `crossposted_views` / `facebook_views` | Reach from FB cross-post |
| `navigation` | Story swipe behavior |

### Account-level insights (NOT YET pulling)
- `profile_views` — daily profile views
- `website_clicks` — bio link clicks (THIS IS THE LINK-CLICK
  CONVERSION METRIC we've been chasing)
- `email_contacts` / `phone_call_clicks` / `text_message_clicks`
- `online_followers` — when your followers are most active (great
  for posting-time optimization)
- `audience_gender_age` — demographic split
- `audience_country` / `audience_city` / `audience_locale`
- `profile_reposts` (account aggregate)

### Marketing API (`ads_read` scope active — NOT YET pulling)
- Ad campaign performance: spend, impressions, clicks, CPM, CPC, CTR
- Per-ad results, custom audiences
- Server-side conversion API

### Stories Insights (NOT YET pulling)
- `exits` — where viewers dropped off
- `replies` — story DM replies
- `reach` / `impressions`
- `taps_forward` / `taps_back` — engagement direction

### Hashtag Search (`instagram_creator_marketplace_discovery`)
- Recent posts under a hashtag
- Top posts under a hashtag (could replace some of the Apify
  competitor scrape for free)

---

## What's still TODO

### This week (immediate, low-hanging)
- [ ] Wire `igInsights` into `daily-strategy.mjs` so Gemini sees
  skip rate per post and can recommend specific hook fixes
- [ ] Surface saves/shares/skip-rate as columns in the dashboard's
  Content Engine → Analytics post table (replace the "SAVES: 0"
  column you've been seeing)
- [ ] Identify the breakout 4,616-reach static post — pull its
  caption and permalink so we know what made 67% of recent
  follower growth
- [ ] Get DJ-specific tracking links from Rose Gold for July 17 +
  August 1 gigs (1-minute DM, unblocks ticket sales)

### Next 2 weeks
- [ ] Add account-level Insights pull: `website_clicks`
  (link-click conversion!), `profile_views`, `online_followers`,
  audience demographics
- [ ] Wire Marketing API for ad spend / CPM / CPC into Paid Media
  tab
- [ ] Stories Insights pull (exits, replies, taps)
- [ ] Build the Higgsfield Paradise visualizer for VO outreach
- [ ] Send Script 3 (VO follow-up) once visualizer is ready
- [ ] Domain check + register `afterthebell.market` ($20)

### Month
- [ ] Trial Reels variant-cycling logging schema (designed in
  follow-up #4 playbook, not yet built)
- [ ] Velocity column on Tracks tab (plays-per-day-since-release —
  data already in Firebase, just unsurfaced)
- [ ] Native-vs-cross-posted TT tracker
- [ ] Release countdown on HQ tab
- [ ] Overdue outreach alerts (`nextFollowUp` < today)
- [ ] Hypeddit chart-position tracking (manual entry to start)
- [ ] Daily-strategy script reads `strategy/playbooks/` so all
  research outputs become primary sources for the AI
- [ ] Verify Trial Reels backdoor unlock at 449 followers (5-min
  IG settings check)

### Longer-term (post-NYC move)
- [ ] Higgsfield Creative Studio tab (Track Visual Kit, Set
  Graphics, Photodump, Mood Library)
- [ ] Style Bible / World Bible builder for AI generation
  consistency
- [ ] Conversion funnel tracker (link-click rate over time + bio
  CTA A/B testing)
- [ ] Ad ROAS rollup tied to organic engagement
- [ ] Photo enhancer panel (Relight + Inpaint + Upscale on real
  phone shots — answers the AI authenticity criticism)
- [ ] App Review submission (only needed if expanding to
  multi-user; solo Dev Mode is fine indefinitely)
- [ ] Run remaining follow-up research prompts (#5 conversion fix,
  #6 Higgsfield production library) when relevant

---

## Operating model now in place

- **Daily 6am ET**: full Apify scrape (IG + TT + SC) + daily-strategy
  Gemini run
- **Daily 6:15am ET**: Graph API IG Insights pull (saves, shares,
  skip rate, follows, profile visits)
- **Daily 6pm ET**: second IG Insights pull for intraday velocity
- **Mondays 6am ET**: full weekly strategy regenerates
- **Recovery cron**: 9:30am, 12pm, 3pm, 6pm ET — re-runs strategy
  if morning attempt failed

This gives the dashboard label-grade analytics depth on Instagram
with no manual work and no recurring cost (Graph API is free).

## What this means strategically

The dashboard now has data that most indie artists don't have
access to without paying Chartmetric / Soundcharts subscriptions.
Specifically: **per-post skip rate, follows-from-post, and
profile-visits-from-post** are diagnostic metrics that turn the
dashboard from a vanity-metrics viewer into an A&R-grade
optimization tool.

The next high-leverage move is wiring this data into the
daily-strategy AI prompt so the strategy stops giving generic
advice ("post more") and starts giving specific advice ("fix the
hook on DANCE. — 76% skip vs your 54% best") with real numbers.
